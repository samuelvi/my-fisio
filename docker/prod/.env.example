# Production Environment Variables
# Copy this file to .env and fill in the values

# Database Configuration (MariaDB)
MARIADB_DATABASE=physiotherapy_db
MARIADB_USER=physiotherapy_user
MARIADB_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
MARIADB_ROOT_PASSWORD=CHANGE_ME_ROOT_PASSWORD_HERE

# Redis Configuration
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD

# Application Secrets
APP_SECRET=CHANGE_ME_RANDOM_SECRET_64_CHARS_MIN
JWT_PASSPHRASE=CHANGE_ME_JWT_PASSPHRASE

# Port Configuration (optional, defaults to 80/443)
HTTP_PORT=80
HTTPS_PORT=443

# IMPORTANT SECURITY NOTES:
# 1. NEVER commit the .env file to git
# 2. Use strong, unique passwords (minimum 32 characters)
# 3. Generate APP_SECRET with: openssl rand -hex 32
# 4. Keep JWT_PASSPHRASE secure and backed up
# 5. Rotate passwords regularly in production
